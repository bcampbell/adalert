update per-site rules to cover new examples
chrome store
(A) client-side mechanism to prevent double-voting
add Access-Control-Allow-Origin header on server
reword: "whitelist" => "list of sites to check"
browser action icon should be monochrome until page is scanned
extract canonical and alternate urls in content.js and use in API calls
use unsourced.org subdomain to host server
options page: disable Save button unless changes pending
