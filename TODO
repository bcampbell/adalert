update per-site rules to cover new examples
add a site whitelist (force lookup of pages on those sites)
chrome version
chrome store
popup results
manual check button for popup
dispute flags
handle displaying multiple warnings (just pick one?)
only send hashed urls for lookups
